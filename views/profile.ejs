<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Postly</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="min-h-screen w-full bg-zinc-950 p-10">
      <div class="flex justify-end">
        <a class="text-red-500 underline-none" href="/logout">Logout</a>
      </div>
      <h1 class="text-white text-3xl">Hey, <%= user.name %> 👋</h1>
      <h3 class="text-white opacity-60 mb-5">you can create a new post.</h3>
      <form
        class="flex flex-row items-center justify-start"
        action="/post"
        method="post"
      >
        <textarea
          class="w-1/3 resize-none bg-zinc-900 border-2 border-zinc-700 rounded-md text-white px-3 py-1 outline-none"
          name="content"
          placeholder="What's on your mind?"
        ></textarea>
        <input
          class="bg-blue-500 rounded-xl px-5 py-2 ml-5 text-white"
          type="submit"
          value="Create Post"
        />
      </form>
      <div class="posts mt-20">
        <h1 class="text-zinc-400 text-2xl mb-5">Your Posts!</h1>
        <div class="post-container flex flex-wrap gap-6">
          <% user.posts.reverse().forEach((post)=>{ %>
          <div
            class="post w-86 p-4 border-2 border-zinc-800 bg-yellow-900/12 text-white rounded-md"
          >
            <h1 class="opacity-30 text-xs tracking-tight"><%= post.date %></h1>
            <p class="tracking-tight"><%= post.content %></p>
            <h1 class="mt-5 text-xs"><%= post.likes.length %> Likes</h1>
            <div class="flex text-xs justify-between mt-1">
              <a class="text-blue-500" href="/like/<%= post._id %>">
                <%= post.likes.indexOf(user._id) === -1 ? "Like" : "Unlike"%>
              </a>
              <a class="text-white opacity-40" href="/edit/<%= post._id %>">Edit</a>
              <a class="text-red-500" href="">Delete</a>
            </div>
          </div>
          <% }) %>
        </div>
      </div>
    </div>
  </body>
</html>
