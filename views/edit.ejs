<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Postly</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
    <div class="min-h-screen w-full bg-zinc-950 p-10">
      <div class="flex justify-end">
        <a class="text-red-500 underline-none" href="/logout">Logout</a>
      </div>
      <h3 class="text-3xl text-white mb-5">Update your post.</h3>
      <form
        class="flex flex-row items-center justify-start"
        action="/update/<%= post._id %>"
        method="post"
      >
        <textarea
          class="w-1/3 resize-none bg-zinc-900 border-2 border-zinc-700 rounded-md text-white px-3 py-1 outline-none"
          name="content"
        ><%= post.content %></textarea>
        <input
          class="bg-yellow-500 rounded-xl px-5 py-2 ml-5 text-black"
          type="submit"
          value="Update Post"
        />
      </form>
    </div>
  </body>
</html>
